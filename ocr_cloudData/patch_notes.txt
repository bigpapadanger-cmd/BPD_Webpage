OCR resilience patch

- Strong unanimous zero topology may override a generic OCR seven.
- A late unresolved zero/seven field defaults to zero, returns an advisory
  warning, and remains a passing field.
- Zero/seven specialist results are authoritative over repeated correlated
  Tesseract variants.
- A narrowly gated high-confidence one-digit score correction can resolve
  conflicts such as 985 versus 585.
- A narrowly gated ping rule can restore a dropped leading digit such as
  8 versus 28.
- Player-title OCR and title properties were removed. Player names remain.
- MVP color candidates now require compact icon-like aspect, fill, and
  solidity so a username-ending X is less likely to be treated as the icon.
- Paddle remains optional at runtime. Local Paddle recovery requires the
  paddleocr package to be installed in the environment running OCR.

Validation performed:
- All included Python files compile successfully.
- Targeted decision tests passed for topology 7-to-0 correction, late
  zero fallback/pass behavior, 585 score selection, and 28 ping selection.
