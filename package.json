{
    "name": "domaindata",
    "version": "1.1.2",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "wrangler pages functions build functions --outdir=.wrangler/pages-build",
        "build:check": "wrangler pages functions build functions --outdir=.wrangler/pages-build",
        "dev": "wrangler pages dev",
        "test": "npm run build:check"
    },
    "devDependencies": {
        "wrangler": "^4"
    }
}