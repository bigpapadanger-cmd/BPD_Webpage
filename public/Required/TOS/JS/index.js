import {
    renderHeader
} from "/Framework/Shell/JS/renderHeader.js";

export function initializePage() {
    renderHeader({
        title:
            "Terms Of Service"
    });

    return true;
}