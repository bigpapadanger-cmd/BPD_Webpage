import {
    renderHeader
} from "/Framework/Shell/JS/renderHeader.js";

export function initializePage() {
    renderHeader({
        title:
            "Frequently Asked Questions"
    });

    return true;
}